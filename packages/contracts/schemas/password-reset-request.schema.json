{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PasswordResetRequest",
  "description": "POST /api/v1/auth/password-reset — api-specification.md §16.1. Always 202, regardless of whether the address exists (§14.11 enumeration protection).",
  "type": "object",
  "additionalProperties": false,
  "required": ["email"],
  "properties": {
    "email": { "type": "string", "format": "email", "maxLength": 320 }
  }
}
