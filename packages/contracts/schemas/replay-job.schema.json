{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReplayJob",
  "description": "POST /api/v1/admin/jobs/{jobId}/replay — api-specification.md §16.22. Replay creates a NEW job carrying the original's lineage; it never mutates the original.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "reason": { "type": "string", "maxLength": 512 }
  }
}
