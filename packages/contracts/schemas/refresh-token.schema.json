{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RefreshToken",
  "description": "POST /api/v1/auth/refresh — api-specification.md §16.1. `refresh_token` is required for API clients; browsers send an empty body and present the refresh token via the `session` cookie instead.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "refresh_token": { "type": "string", "minLength": 1, "maxLength": 512 }
  }
}
